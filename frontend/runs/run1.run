color BLUE

drive_backward(distance=100 speed=100)

set_event (index=1)

tasksplit {
    module_right_cw(distance=5000 speed=100)
    module_right_cw(distance=1000 speed=100)

    trigger_event (index=1)
}

tasksplit {
    module_left_cw(distance=2000 speed=100)
    module_left_ccw(distance=2000 speed=100)

    trigger_event (index=1)
}

drive_forward_gyro(distance=1000 speed=40 accel_distance=0)

wait_event(index=1)

turn_left(angle=90 speed=50)
