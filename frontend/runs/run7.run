color WHITE

drive_forward_gyro(distance=980 speed=100)
turn_left_on_spot(angle=52 speed=20)
drive_forward_gyro(distance=1080 speed=80)

tasksplit {
    module_left_cw(distance=150 speed=10)
}

drive_forward_time_gyro(time=300 speed=10)

wait_global_timer(index=0 time=14800)

// Solve Uboot
module_right_cw(distance=650 speed=100)

// Wait to match end
drive_backward_gyro(distance=150 speed=30)
